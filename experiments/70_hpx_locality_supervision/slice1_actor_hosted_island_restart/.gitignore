# exp70 Slice 1 -- EXPERIMENT-ONLY run artifacts stay untracked (curated aggregate is the only
# tracked evidence, added after acceptance). Mirrors exp68 hygiene.
build/
build*/
_exp70_slice1_runs/
__pycache__/
*.so
*.dylib
*.o
*.pyc

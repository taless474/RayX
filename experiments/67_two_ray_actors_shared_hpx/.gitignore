# exp67 -- EXPERIMENT-ONLY build/run artifacts stay untracked (curated aggregate is the only
# tracked evidence, added after acceptance). Mirrors exp66 hygiene.
build/
build*/
_exp67_runs/
__pycache__/
*.so
*.dylib
*.o
*.pyc
